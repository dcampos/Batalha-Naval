package batalhanaval;

import batalhanaval.gui.*;

public class Principal {

	/**
	 * @param args
	 */
	public static void main(String[] args) {
		Jogo novoJogo = new Jogo();
/*
		try {
			for (int i=0; i<10; i++)
				novoJogo.getJogador(1).atira();
		} catch (Exception e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
*/
		JanelaPrincipal principal = new JanelaPrincipal(novoJogo);
		principal.pack();
		principal.setVisible(true);

		/*
		 * System.out.println(novoJogo.getJogador(0).getTabuleiro().toString());
		 * 
		 * while (novoJogo.estaTerminado() == false) {
		 * System.out.println("Digite a coluna do tiro: "); int col =
		 * in.nextInt(); System.out.println("Digite a linha do tiro: "); int
		 * linha = in.nextInt(); try { System.out.println("Resultado: " +
		 * novoJogo.getJogador(0).atira(col, linha));
		 * System.out.println("Frota inimiga restante: " +
		 * novoJogo.getJogador(1).getFrotaRestante());
		 * System.out.println(novoJogo.getJogador(1).getTabuleiro());
		 * novoJogo.getJogador(1).atira();
		 * System.out.println(novoJogo.getJogador(0).getTabuleiro()); } catch
		 * (Exception e) { System.out.println(e.getMessage()); } }
		 */
	}
}
